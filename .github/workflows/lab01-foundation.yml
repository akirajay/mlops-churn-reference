name: Lab 01 - Foundation

on:
  push:
    paths: [ 'infra/**', '.github/workflows/lab01-foundation.yml' ]
    branches: [ main ]
  pull_request:
    paths: [ 'infra/**' ]
  workflow_dispatch:

permissions:
  id-token: write     # OIDC
  contents: read

# 防止连续 push 触发并发部署互相覆盖；部署中途不取消以免留下半成品资源
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_OIDC_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_OIDC_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_OIDC_SUBSCRIPTION_ID }}
      - name: Bicep build
        run: az bicep build --file infra/main.bicep
      - name: What-if (dev)
        run: |
          az deployment group what-if -g rg-churn-dev \
            -f infra/main.bicep -p infra/env/dev.bicepparam

  deploy-shared:
    needs: validate
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_OIDC_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_OIDC_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_OIDC_SUBSCRIPTION_ID }}
      - run: |
          az deployment group create -g rg-churn-shared \
            -f infra/shared.bicep --query properties.provisioningState

  deploy-dev:
    needs: deploy-shared
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_OIDC_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_OIDC_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_OIDC_SUBSCRIPTION_ID }}
      - run: |
          az deployment group create -g rg-churn-dev \
            -f infra/main.bicep -p infra/env/dev.bicepparam \
            --query properties.provisioningState

  deploy-prod:
    needs: deploy-dev
    runs-on: ubuntu-latest
    # prod 审批门禁：environment 的 Required reviewers 会在此暂停等待人工批准。
    # OIDC subject 变为 repo:<owner/repo>:environment:prod，对应已存在的 federated credential。
    environment: prod
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_OIDC_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_OIDC_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_OIDC_SUBSCRIPTION_ID }}
      - run: |
          az deployment group create -g rg-churn-prod \
            -f infra/main.bicep -p infra/env/prod.bicepparam \
            --query properties.provisioningState
